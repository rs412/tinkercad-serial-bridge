# Tinkercad Serial Bridge

一款 Manifest V3 浏览器插件，为 Tinkercad Arduino 仿真提供双向串口数据桥接：把仿真串口输出转发给本地/远程服务，并把外部指令写回仿真串口。

支持自定义服务地址、一键启停、实时运行状态、多标签页防抢指令，配置自动保存。

---

## ✨ 功能特性

- **双向串口通信**：串口输出实时上行，外部指令实时下行到仿真
- **增量上报**：只上报新增行，串口快速打印多行时不再只留最后一行
- **服务层统一出口**：网络请求集中在 `background.js`，避开页面 CORS 限制，便于重试与诊断
- **运行状态面板**：服务在线/离线、页面连接数、最近上下行内容与时间、页面元素检测结果
- **多标签页防抢指令**：多个 Tinkercad 标签页同时打开时，只有一个 leader 拉取指令，避免队列被互抢
- **选择器兜底**：内置多套选择器 + iframe 扫描 + 自定义选择器配置，Tinkercad 改版后无需更新插件
- **一键自检**：测试连接、手动下发指令，快速定位是服务端、网络还是页面元素的问题
- **配置持久化**：所有设置自动保存，重启浏览器不丢失

---

## 📁 项目结构

```
tinkercad-serial-bridge/
├── manifest.json     # 插件核心配置
├── background.js     # 服务层：网络出口、状态聚合、leader 选举
├── content.js        # 页面层：串口文本增量抓取、指令写入与发送
├── popup.html        # 配置与状态面板
├── popup.js          # 面板逻辑
├── icons/            # 16 / 48 / 128 图标
├── sample/           # 完整可运行示例（服务端 + 控制页 + Arduino 代码）
├── docs/             # 商店截图与源图（不参与插件打包逻辑）
└── README.md
```

---

## 🔧 安装教程

1. 将项目文件夹下载/克隆到本地

2. 打开 Chrome / Edge 浏览器扩展管理页面

3. 开启页面「开发者模式」

4. 点击「加载已解压的扩展程序」，选中项目文件夹即可安装

> 修改代码后，回到扩展管理页点击插件卡片上的「重新加载」即可生效。

---

## ⚙️ 使用方法

### 1. 启动中转服务

进入 `sample/` 目录，执行：

```bash
node led.js
# 或指定端口：PORT=9000 node led.js
```

### 2. 插件参数配置

点击浏览器插件图标，打开配置面板：

- **服务地址**：默认 `http://localhost:8080`，会自动去掉结尾多余的 `/`；填写非 localhost 地址时会弹出授权请求
- **上行间隔**：读取串口监视器的间隔，默认 800ms（串口内容变化时也会立即上报，不等轮询）
- **指令轮询间隔**：向服务端拉取待发指令的间隔，默认 2000ms
- **行过滤**：填写后只有包含该字符串的行才会上报，用于过滤仿真自带的调试输出，留空则全部上报
- **启用桥接**：总开关
- **高级 → 自定义选择器**：Tinkercad 改版导致自动探测失效时，手动填写监视器 / 输入框 / 发送按钮的 CSS 选择器

点击「保存设置」立即生效，无需刷新页面。

### 3. 正常运行

打开 Tinkercad Arduino 仿真项目并运行，面板上「Tinkercad 页面」显示已连接、「元素检测」三项全为 ✓ 即可。

### 4. 状态与排障

面板各项含义：

| 项目 | 正常表现 | 异常排查 |
| --- | --- | --- |
| 中转服务 | 在线 · /health | 点「测试连接」，若失败检查服务是否启动、地址端口是否正确 |
| Tinkercad 页面 | 已连接 N 个标签页 | 未连接说明仿真页面没打开或插件未重新加载 |
| 元素检测 | 监视器✓ 输入✓ 发送✓ | 有 ✗ 说明 Tinkercad 改版，去「高级」填自定义选择器 |
| 最近上行 | 时间 + 内容 | 长期为「—」检查行过滤是否过严 |
| 最近下行 | 时间 + 内容 | 长期为「—」说明服务端队列里没有指令 |
| 元素检测尾部 | 服务层 / 页面直连 | 显示「页面直连」说明 background 连续不可达已降级，功能仍可用，但建议重载扩展 |

---

## 🔌 服务端接口

插件兼容以下接口，`sample/led.js` 已全部实现，也可以用自己的服务替换。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/send?out=<line>` | 上行一条串口输出（**原样中转**，一行一个请求） |
| GET | `/cmd` | 取出一条待发指令，队列为空返回空字符串 |
| POST | `/cmd` | 下发指令入队，支持纯文本或 `{"cmd":"..."}` |
| GET | `/getLog` | 返回最新一条串口输出（兼容旧前端） |
| GET | `/log?n=50&since=0` | 返回 JSON 环形日志 `{seq, latest, lines[]}` |
| GET | `/health` | 健康检查，供插件自检 |
| POST | `/reset` | 清空指令队列与日志 |

---

## 📝 更新日志

### 26.9.7

- 新增 `background.js` 服务层，网络请求统一出口，支持任意服务地址
- 修复 React 受控输入框写入：改用原型原生 setter，并在点击发送前等待 300ms 让 React 提交 state（原版之所以能用就是靠这个等待）
- background 连续 3 次不可达时自动降级为页面内直连，收发行为与旧版插件一致，新增的服务层不会成为单点故障
- 串口上行改为增量 diff，不再只上报最后一行；串口监视器顶部裁剪也能正确对齐
- 多标签页 leader 选举，避免 `/cmd` 队列被互抢
- 元素查找增加多套兜底选择器、同域 iframe 扫描、自定义选择器配置
- 用 MutationObserver 即时上报，不再依赖轮询延迟
- 新增状态面板：服务/页面连接状态、最近收发、元素检测、测试连接、手动下发
- 参数校验（地址归一化、间隔 clamp）、非本地地址按需申请权限
- 服务端新增环形日志、`/log`、`/health`、队列上限，同时完整保留旧接口
- 图标导出真实三尺寸；截图与备份文件移入 `docs/`

### 26.8.13

- 支持自定义服务地址、上行与轮询间隔、一键启停

---

## ✅ Author Info

  Developer: rs412

  Email: redshift@yeah.net

  Github: https://github.com/rs412/
